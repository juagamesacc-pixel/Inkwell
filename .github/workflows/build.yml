name: Build Android APK

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: inkwell_app
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'
          channel: 'stable'
          cache: true

      - name: Remove existing android dir
        run: rm -rf android

      - name: Create Flutter project structure
        run: |
          flutter create --org com.inkwell --project-name inkwell temp_project
          cp -r temp_project/android .
          rm -rf temp_project

      - name: Get dependencies
        run: flutter pub get

      - name: Run build_runner (if needed)
        run: flutter pub run build_runner build --delete-conflicting-outputs || true

      - name: Analyze code
        run: flutter analyze || true

      - name: Build APK (arm64-v8a only)
        run: flutter build apk --release --target-platform android-arm64

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: inkwell-apk-arm64
          path: inkwell_app/build/app/outputs/flutter-apk/*.apk
          retention-days: 30

      - name: Create Release
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ github.run_number }}
          name: Inkwell v${{ github.run_number }}
          body: |
            ## What's New
            - Bug fixes and improvements
            
            ## Architecture
            - arm64-v8a only
          files: inkwell_app/build/app/outputs/flutter-apk/*.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
